<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="currency"
            type="com.script972.currencyrate.api.model.CurrencyResponce" />

        <variable
            name="listener"
            type="com.script972.currencyrate.ui.adapters.CurrencyAdapter.OnClickListener" />

    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <LinearLayout
            android:id="@+id/ll_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/circle_item_list"
            android:gravity="center"
            android:onClick="@{()->listener.onClickItem(currency)}"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txt_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{String.valueOf(currency.rate)}"
                android:textSize="24sp" />

            <TextView
                android:id="@+id/txt_currency"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{currency.titleShort}"
                android:textSize="22sp" />

        </LinearLayout>


    </LinearLayout>

</layout>